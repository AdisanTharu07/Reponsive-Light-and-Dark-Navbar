// dark and light code
function darkBtn() {
    const toggle = document.body;
    toggle.classList.toggle('darkmode');
}

// open nav code
var showMenu = document.getElementById('showMenu');
function menuBtn() {
    showMenu.classList.toggle('open__nav');
}